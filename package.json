{
  "name": "example-typescript",
  "version": "1.0.0",
  "description": "a typescript example",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "npm run tsc && npm run webdriver-update && npm run test",
    "test": "protractor conf.js",
    "debug": "node --inspect-brk ./node_modules/.bin/protractor conf.js",
    "tsc": "tsc",
    "webdriver-update": "webdriver-manager update --standalone=false --gecko=false"
  },
  "dependencies": {
    "@types/jasmine": "^3.3.12",
    "jasmine": "^3.3.1",
    "protractor": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.4.10",
    "@types/selenium-webdriver": "^4.0.0",
    "jasmine-spec-reporter": "^7.0.0",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
